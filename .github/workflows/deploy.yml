name: Deploy WordPress

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "$sayalildap.pem" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Install Dependencies
        run: |
          # Install any dependencies here
          echo "Installing dependencies..."

      - name: Deploy WordPress
        run: |
          # Deploy WordPress using your deployment script
          # For example, use SCP to transfer files to the VPS
          scp -r ./wordpress/* ubuntu@ec2-35-169-124-160/path/to/destination

      - name: Cleanup SSH Key
        run: |
          rm -f ~/.ssh/id_rsa

